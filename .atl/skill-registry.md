# Skill Registry

Generated by `sdd-init` on 2026-05-06. This file lists user-level skills available
in this project and project-level conventions, so SDD phases (and future agents)
know what they can call.

## Project Conventions

- `~/.claude/rules/sdd-orchestration.md` — orchestrator-pattern SDD workflow (mandatory for non-trivial work)
- `~/.claude/rules/context7.md` — for any library/framework/SDK/CLI question, resolve-library-id then query-docs before writing code

No project-level `CLAUDE.md` / `AGENTS.md` / `.cursorrules` yet. To be added in Phase 0.

## User-Level Skills (relevant to Fragwise)

### Build / Implementation
- `frontend-design` — distinctive, production-grade frontend interfaces; avoids generic AI aesthetics. Use for any apps/web component or page work.
- `claude-api` — Claude API / Anthropic SDK best practices. (Limited use here — primary LLM is OpenAI — but useful reference patterns.)

### Discovery / Documentation
- `context7-mcp` — fetch current library/framework docs via Context7 MCP. Triggered automatically per the global rule.
- `find-skills` — discover and install additional skills from the open ecosystem.

### Workflow / Quality
- `simplify` — review changed code for reuse, quality, efficiency.
- `judgment-day` — adversarial dual review when changes need a hard second opinion.
- `branch-pr` — PR creation workflow (issue-first).
- `issue-creation` — GitHub issue workflow.
- `fewer-permission-prompts` — reduce permission prompts by allowlisting common reads.
- `update-config` — settings.json / hooks management.

### SDD Pipeline (run via Skill tool)
- `sdd-init` (done), `sdd-explore`, `sdd-propose`, `sdd-spec`, `sdd-design`, `sdd-tasks`, `sdd-apply`, `sdd-verify`, `sdd-archive`.

## Skills Skipped

- `_shared` (internal only)
- `skill-registry` (this file generator)
- `keybindings-help`, `loop`, `schedule` (out of scope for project work)
- `go-testing` (project is Python + TS, no Go)
